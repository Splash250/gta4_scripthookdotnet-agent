# GetAllPeds Method

Returns all currently existing peds. SLOW! Also includes players, mission peds and vehicle passengers! Make sure to check every returned Ped with Exists() before using it!

#### Overload List

Returns all currently existing peds. SLOW! Also includes players, mission peds and vehicle passengers! Make sure to check every returned Ped with Exists() before using it!

> public static GTA.Ped[] GetAllPeds();

Returns all currently existing peds of one model. Slow, but very reliable! Also includes players, mission peds and vehicle passengers! Make sure to check every returned Ped with Exists() before using it!

> public static GTA.Ped[] GetAllPeds(GTA.Model);

#### See Also

[World Class](World.md) | [GTA Namespace](index.md)
